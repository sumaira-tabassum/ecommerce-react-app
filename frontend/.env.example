# Production: API is on HTTP port 80 — use IP or domain only (no :port in the URL).
# Example: VITE_BASE_URL=http://203.0.113.10
#
# Local dev when the backend runs on Node’s default port:
# VITE_BASE_URL=http://localhost:3000

VITE_BASE_URL=